<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/color_logo_orange</item>
        <item name="colorPrimaryDark">@color/color_logo_orange</item>
        <item name="colorAccent">@color/color_red</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/color_logo_orange_dark</item>
    </style>

    <!-- No action bar -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Splash screen -->
    <style name="AppTheme.SplashScreen">
        <item name="android:statusBarColor">@color/color_logo_orange</item>
    </style>

    <!-- Buttons -->
    <style name="AppTheme.ButtonOutlined" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/color_green</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/color_green</item>
    </style>

    <style name="AppTheme.SwitchButton">
        <item name="colorAccent">@color/color_green</item>
    </style>

    <!-- BottomNavigationView -->
    <style name="AppTheme.BottomNavigationView">
        <item name="colorPrimary">@color/color_green</item>
        <item name="colorOnPrimary">@color/color_green</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
